package mcp

const (
	// ContextKeyAuthToken is the key used to store/retrieve the auth token from context
	ContextKeyAuthToken = "auth_token"
	// ContextKeyUser is the key used to store/retrieve the user object from context
	ContextKeyUser = "user"
)
